# 🚀 دستورالعمل سریع - اضافه کردن خطا

## روش ۱: از طریق GitHub Copilot (توصیه می‌شود) ⭐

وقتی خطای جدیدی پیدا کردید، **همینجا توی Copilot Chat بنویسید:**

```
این خطا رو به سیستم رفع خطاها اضافه کن:

عنوان: [عنوان خطا با ایموجی]
خطا: [متن خطا]
توضیح: [توضیح کوتاه]
راه حل: [مراحل حل]
تگ‌ها: [تگ‌ها با کاما]
```

**مثال:**
```
این خطا رو به سیستم رفع خطاها اضافه کن:

عنوان: 🔴 خطای اتصال به PostgreSQL
خطا: could not connect to server: Connection refused
توضیح: PostgreSQL نصب نیست یا اجرا نمی‌شود
راه حل:
1. PostgreSQL را نصب کنید
2. سرویس را استارت کنید: net start postgresql-x64-16
3. از pgAdmin وضعیت را چک کنید
تگ‌ها: PostgreSQL, Connection, نصب
```

من خودکار:
- ✅ خطا رو به `errors.json` اضافه می‌کنم
- ✅ ID یکتا می‌سازم
- ✅ کلمات کلیدی رو استخراج می‌کنم
- ✅ تاریخ رو ثبت می‌کنم

بعدش فقط بگو: **"الان آپلودش کن"** و من مستقیم به GitHub آپلودش می‌کنم!

---

## روش ۲: از طریق Python (دستی)

```powershell
cd D:\business\odoo\odoo-troubleshoot-guide
python add_error.py
```

**مزایا:**
- کنترل کامل روی همه فیلدها
- امکان اضافه کردن چند راه حل
- امکان اضافه کردن چند مرحله برای هر راه حل

**معایب:**
- کمی وقت‌گیر
- باید همه سوالات رو جواب بدید

---

## روش ۳: افزودن + آپلود یکجا

```powershell
cd D:\business\odoo\odoo-troubleshoot-guide
.\quick_add_and_upload.ps1
```

این روش:
1. اول خطا رو اضافه می‌کنه
2. بعد می‌پرسه آپلود کنه یا نه

---

## 📤 فقط آپلود (بدون افزودن خطا)

```powershell
# PowerShell
.\upload_to_github.ps1

# Git Bash / Linux
bash upload.sh
```

---

## ⚡ دستورات فوری

```powershell
# مشاهده تعداد خطاها
python -c "import json; print(len(json.load(open('errors.json'))))"

# مشاهده آخرین خطا
python -c "import json; e=json.load(open('errors.json'))[-1]; print(f'{e[\"id\"]}: {e[\"title\"]}')"

# لیست عناوین همه خطاها
python -c "import json; [print(f'{i+1}. {e[\"title\"]}') for i,e in enumerate(json.load(open('errors.json')))]"
```

---

## 🎯 بهترین روش؟

| حالت | روش پیشنهادی |
|------|-------------|
| یک خطا رو سریع اضافه کنم | **Copilot Chat** ⭐ |
| چند خطا رو یکجا اضافه کنم | Python Script |
| کنترل کامل می‌خوام | Python Script |
| خطا رو خودم اضافه کردم، فقط آپلود | `upload_to_github.ps1` |

---

## 🌐 مشاهده نتیجه

بعد از آپلود (۲-۳ دقیقه صبر کنید):

👉 **https://shehneh.github.io/odoo-troubleshoot-guide/**

---

**نکته:** همیشه قبل از آپلود، `index.html` رو باز کنید و ببینید خطا درست نمایش داده میشه یا نه!
