# 🔧 سیستم مدیریت خطاهای Odoo 19

یک سیستم کامل برای جمع‌آوری، مدیریت و نمایش خطاهای رایج Odoo با رابط کاربری زیبا و جستجوی هوشمند.

## 🌐 مشاهده آنلاین

👉 **[https://shehneh.github.io/odoo-troubleshoot-guide/](https://shehneh.github.io/odoo-troubleshoot-guide/)**

---

## 📁 ساختار فایل‌ها

```
odoo-troubleshoot-guide/
├── index.html                  # صفحه اصلی با UI
├── errors.json                 # دیتابیس خطاها (JSON)
├── errors-loader.js            # JavaScript برای بارگذاری و نمایش
├── add_error.py                # اسکریپت اضافه کردن خطای جدید
├── upload_to_github.ps1        # آپلود به GitHub
├── quick_add_and_upload.ps1    # اضافه کردن + آپلود یکجا
└── README.md                   # این فایل
```

---

## 🚀 نحوه استفاده

### روش ۱: افزودن خطای جدید (تعاملی)

```powershell
# وارد پوشه شوید
cd D:\business\odoo\odoo-troubleshoot-guide

# اجرای اسکریپت Python
python add_error.py
```

**سوالاتی که از شما پرسیده می‌شود:**
1. ✏️ عنوان خطا (مثلاً: `🔴 خطای اتصال به PostgreSQL`)
2. 🎨 نوع خطا (error/warning/info/success)
3. ❌ متن خطای اصلی (کپی-پیست از ترمینال)
4. 📄 توضیح کوتاه
5. 🏷️ تگ‌ها
6. 🔑 کلمات کلیدی
7. 💡 راه حل‌ها (گام به گام)

---

### روش ۲: افزودن سریع + آپلود یکجا

```powershell
.\quick_add_and_upload.ps1
```

این اسکریپت:
- ✅ خطای جدید رو اضافه می‌کنه
- ✅ بعد از افزودن، از شما می‌پرسه آپلود کنه یا نه
- ✅ اگه بگید بله، مستقیم به GitHub آپلود می‌کنه

---

### روش ۳: آپلود دستی به GitHub

```powershell
# آپلود با پیام پیش‌فرض
.\upload_to_github.ps1

# آپلود با پیام سفارشی
.\upload_to_github.ps1 "اضافه کردن ۵ خطای جدید PostgreSQL"
```

---

## 📝 مثال: اضافه کردن یک خطای واقعی

```powershell
python add_error.py
```

```
📝 عنوان خطا: 🔴 خطای Access Denied در دیتابیس
🎨 نوع خطا: 1 (error)
❌ متن خطای اصلی:
psql: error: FATAL:  password authentication failed for user "odoo"

📄 توضیح: این خطا زمانی رخ می‌دهد که رمز عبور در odoo.conf با PostgreSQL مطابقت ندارد
🏷️ تگ‌ها: PostgreSQL,Authentication,Access Denied
🔑 کلمات کلیدی: password,authentication,failed,odoo,postgresql

💡 راه حل 1: بررسی رمز عبور
  مرحله 1: فایل odoo.conf را باز کنید
  کد: notepad "C:\Program Files\Odoo 19.0\server\odoo.conf"
  
  مرحله 2: رمز db_password را چک کنید
  
  مرحله 3: با rg Admin رمز را ریست کنید
```

✅ خطا با موفقیت اضافه شد!

---

## 🎯 ویژگی‌های سیستم

### ✨ برای کاربران:
- 🔍 **جستجوی هوشمند**: جستجو در عنوان، متن خطا، کلمات کلیدی
- 🎨 **رنگ‌بندی**: خطاها با رنگ‌های مختلف (قرمز، نارنجی، آبی، سبز)
- 📋 **کپی با یک کلیک**: کپی کردن دستورات به راحتی
- 🏷️ **تگ‌گذاری**: فیلتر سریع با تگ‌ها
- 📱 **رسپانسیو**: کار روی موبایل، تبلت، دسکتاپ

### 🛠️ برای توسعه‌دهندگان:
- 📦 **JSON-based**: همه خطاها در یک فایل JSON
- 🐍 **Python CLI**: اضافه کردن خطا به صورت تعاملی
- 🚀 **Auto-deploy**: آپلود خودکار به GitHub Pages
- 🆔 **Auto-ID**: تولید خودکار شناسه یکتا
- 📅 **Timestamp**: ثبت تاریخ افزودن خطاها

---

## 🔄 جریان کار (Workflow)

```
[یافتن خطای جدید] 
      ↓
[اجرای add_error.py]
      ↓
[پر کردن اطلاعات]
      ↓
[ذخیره در errors.json]
      ↓
[آپلود به GitHub]
      ↓
[نمایش در وبسایت]
```

---

## 📊 فرمت JSON

هر خطا با این ساختار ذخیره می‌شود:

```json
{
  "id": "error-unique-id-260207",
  "title": "🔴 عنوان خطا",
  "type": "error",
  "errorText": "متن خطای اصلی...",
  "description": "توضیح کوتاه",
  "keywords": ["کلمه۱", "کلمه۲"],
  "tags": ["تگ۱", "تگ۲"],
  "solutions": [
    {
      "title": "راه حل ۱",
      "steps": [
        {
          "number": 1,
          "text": "مرحله اول...",
          "code": "دستور..."
        }
      ]
    }
  ],
  "dateAdded": "2026-02-07T12:30:00"
}
```

---

## 🎬 دستورات سریع

```powershell
# افزودن خطای جدید
python add_error.py

# افزودن + آپلود
.\quick_add_and_upload.ps1

# فقط آپلود
.\upload_to_github.ps1

# مشاهده لیست خطاها
python -c "import json; print('\n'.join([e['title'] for e in json.load(open('errors.json'))]))"

# تعداد خطاها
python -c "import json; print(f'{len(json.load(open(\"errors.json\")))} errors')"
```

---

## 🌟 نکات مهم

1. **قبل از آپلود**: حتماً خطا رو تست کنید (index.html رو باز کنید)
2. **کلمات کلیدی**: حتماً کلمات انگلیسی و فارسی رو اضافه کنید
3. **کدها**: همیشه کدها رو تست کنید قبل از اضافه کردن
4. **توضیحات**: واضح و مختصر بنویسید

---

## 🤝 مشارکت

اگر خطای جدیدی پیدا کردید:

1. Fork کنید
2. خطا رو با `add_error.py` اضافه کنید
3. Pull Request بزنید

---

## 📞 پشتیبانی

اگر مشکلی داشتید:
- 💬 Issue بزنید توی GitHub
- 📧 ایمیل بزنید

---

## 📝 لایسنس

MIT License - استفاده آزاد برای همه!

---

**ساخته شده با ❤️ برای جامعه Odoo ایران**
